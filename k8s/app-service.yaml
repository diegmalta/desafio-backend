apiVersion: v1
kind: Service
metadata:
  name: desafio-backend
  namespace: desafio-notif
  labels:
    app.kubernetes.io/name: desafio-backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/name: desafio-backend
